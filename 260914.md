RNN

길이 50에서 데이터를 줄여보면 

학습데이터가 줄수록 ........
데이터 양이 줄어든거라 정확도가 줄어들었음 

ㅇㅋ ㅋㅋ **그대로 복붙해서 `.md` 파일에 넣을 수 있는 형태**로 줄게.

```
# RNN에서 W와 Hidden State의 차이

RNN에서 **W는 가중치(weight)**이고, **hidden state는 $h_t$**이다.

## 기본적인 RNN 식

$$
h_t = \tanh(W_{xh}x_t + W_{hh}h_{t-1} + b)
$$

각각의 의미는 다음과 같다.

| 기호 | 의미 |
|---|---|
| $x_t$ | 현재 입력값 |
| $h_{t-1}$ | 이전 **Hidden State** |
| $h_t$ | 현재 **Hidden State** |
| $W_{xh}$ | 입력 → Hidden으로 가는 **가중치** |
| $W_{hh}$ | 이전 Hidden → 현재 Hidden으로 가는 **가중치** |
| $b$ | Bias |

## 핵심

**W와 h는 완전히 다른 개념이다.**

- **W = Weight(가중치)**
  - 학습을 통해 업데이트되는 값
  - 입력이나 이전 Hidden State를 얼마나 반영할지 결정

- **h = Hidden State**
  - RNN이 현재까지의 정보를 담고 있는 상태
  - 입력이 들어올 때마다 계산되어 바뀜

쉽게 비유하면:

> **W = 정보를 어떻게 반영할지 정해놓은 학습 가능한 규칙**
>
> **h = 그 규칙을 이용해서 현재까지 기억하고 있는 정보**

## 특히 중요한 $W_{hh}$

RNN에서 중요한 가중치 중 하나가 **$W_{hh}$**이다.

이 가중치는 이전 Hidden State $h_{t-1}$를 현재 Hidden State $h_t$로 전달할 때 사용된다.

$$
h_t = f(W_{xh}x_t + W_{hh}h_{t-1} + b)
$$

따라서 다음과 같이 기억하면 된다.

$$
\boxed{W = \text{학습되는 가중치}}
$$

$$
\boxed{h = \text{계산되는 Hidden State}}
$$

## 한 줄 요약

**W는 학습되는 값이고, h는 W를 이용해서 매 시점마다 계산되는 현재의 상태(state)이다.**
```
