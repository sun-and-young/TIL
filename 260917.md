
## `nn.Sequential` 주요 기능

**핵심**: 계층을 순서대로 쌓는 **컨테이너**로, 각 층의 출력이 다음 층의 입력으로 자동 전달됩니다.

### 생성 방식 (2가지)

```python
import torch.nn as nn

# 1) 모듈을 직접 전달
model = nn.Sequential(
    nn.Linear(784, 128),
    nn.ReLU(),
    nn.Linear(128, 10)
)

# 2) OrderedDict로 이름 붙여 전달 (디버깅 용이)
from collections import OrderedDict
model = nn.Sequential(OrderedDict([
    ('fc1', nn.Linear(784, 128)),
    ('relu', nn.ReLU()),
    ('fc2', nn.Linear(128, 10))
]))
```

### 주요 메서드

| 메서드 | 기능 |
|---|---|
| `append(module)` |末尾에 모듈 추가 |
| `insert(index, module)` | 특정 위치 삽입 |
| `__getitem__` | 인덱스/슬라이스로 접근 |
| `__len__` | 모듈 개수 반환 |

### `forward` 동작

`forward()`를 **직접 구현할 필요 없이** 자동 연쇄 실행:
```python
x = model(x)  # fc1 → relu → fc2 순으로 자동 실행
```

### `nn.ModuleList`와의 차이

| | `nn.Sequential` | `nn.ModuleList` |
|---|---|---|
| `forward()` | ✅ 자동 연쇄 실행 | ❌ 없음 (수동 for 루프 필요) |
| 용도 | 단순 직렬 구조 | 반복/분기 등 **임의** 호출 패턴 |
| 파라미터 등록 | ✅ | ✅ |

### 한계

- **분기·스킵 연결**(ResNet, U-Net 등) 불가 → 커스텀 `nn.Module`의 `forward()` 필요
- 단일 입력 → 단일 출력만 지원
- 복잡한 데이터 플로우가 필요하면 `nn.Module` 직접 상속

