 
싸이킷런 iris
1-2 펭귄문제 
02 iris
2-1 실습문제 펭귄



## EDA 체크리스트 정리

새로운 데이터셋을 만나면 이 순서로:

| 순서 | 코드 | 확인하는 것 |
|------|------|-------------|
| 1 | `df.shape` | 몇 행, 몇 열? |
| 2 | `df.dtypes` / `df.info()` | 각 컬럼의 데이터 타입 |
| 3 | `df.isnull().sum()` | 결측치 있나? |
| 4 | `df.describe()` | 기본 통계 (평균, 표준편차, 최솟값, 최댓값) |
| 5 | `df["target"].value_counts()` | 클래스 분포 균등한가? |
| 6 | 산점도 / 박스플롯 / 히스토그램 | 시각적 패턴 확인 |
| 7 | `df.corr()` + 히트맵 | 피처 간 상관관계 |

> 이 과정 없이 모델을 돌리면, **왜 성능이 안 나오는지 알 수 없습니다.**

----몰랐던 코드 ----

펭귄 데이터셋에서... 
print(f"데이터 크기: {peng_raw.shape}") 
peng_raw.shape를 중괄호로 감쌀수있다. 그 안에 코드를 작성할수있다 >>> f-string 특징임

[] 사용법 

if row["island"] == "Torgersen":
        return "Adelie" 여기서 대괄호 넣은거 뭐야 파이썬 문법에선 못보던건데 대괄호 넣을수 있었던거야? 지금 펭귄 데이터셋 배우는중

if row["island"] == "Torgersen":

는

row의 island 값이 "Torgersen"과 같은가?

 []는 언제 쓰는 거야?
파이썬에서 []는 크게 보면 **"여기서 뭔가를 꺼내거나 접근한다"**고 생각하면 편해요.
리스트
[0] [1] [2]
 ↑
위치(인덱스)로 찾음


딕셔너리
["island"] ["species"]
     ↑
키(key)로 찾음


-------------- 
GridSearchCV(
    model,
    params,
    cv=5,
    return_train_score=True
)
#return_train_score=True, 같은 키워드 인자는 맨 마지막에 넣기

------

결측치 비율 구하기 
print(f"\n전체 결측치 비율: {df.isnull().sum().sum()/ (df.shape[0] * 100)}")

peng_raw[0] 에서 [0]
peng_raw.shape[0]  # 행 개수
peng_raw.shape[1]  # 열 개수
shape = (344, 8)
          ↑    ↑
        [0]  [1]

peng_raw.shape[0] : peng_raw의 행이 몇 개냐 묻는것 
 



--용어 및 모르는 것 --

하이퍼파라미터 
학습을 하기전에 사람이 미리 정해줘야 하는 변수 또는 파라미터들.
alpha(손실 함수의 정규화 항에 곱해지는 상수), max_iter(경사 하강을 몇번할것인가 정하는 값)는 옵셔널 파라미터
이 두 값은 모델이 직접 학습해서 배우는게 아니라 모델을 만드는 사람이 정해주어야 한다.
사이킷 런에서는 보통 모델을 만들 때 옵셔널 파라미터로 정해주는 변수들임
하이퍼파라미터의값에 따라 모델의 성능에 큰 차이가 있을 수 있기 때문에 모델의 성능을 높여주는 좋은 하이퍼파라미터를 구해야하는데
좋은 하이퍼 파라미터를 고르는 방법중 하나에 그리드 서치가 있음.

그리드서치
정해줘야하는 각 하이퍼파라미터에 넣어볼 후보값을 몇개씩 정하는데, 모든 후보값의 조합으로 모델을 학습 시켰을때 성능이 가장 좋았던
 하이퍼 파라미터의 조합을 고른다

(정해야할 맥스이터의 값을 정할때 모르겠으면 구글링을 해서 sklearn에 사용되는 디폴드값을 찾아봐야한다)
 
이상치

hold out

넘파이에서 차원?

pca?
최적 cv?
음의 상관? 양의 상관?

#모델셀렉션에 트레인셀렉션이 있는 이유 :  
 
알고리즘마다 하이퍼파라미터값이다르다 
알고리즘마다 넣는게 다르다
값에 따라서도 성능이 달라질수있다.

--몰랐던것 --

random_state값을 바꿔도 결과가 같았던 이유:
 - 숫자를 바꿨을 때 결과가 바뀌게 만드는 방법 (실험용)
random_state를 쓰는 이유, 써야할 때와 안쓰는 때:
- 

train_test_split 나눌때 stratify쓰는 이유는 분류 때문(회귀와 분류에서)
분류에선 이 옵션이 필수이다

 
> **박스플롯 읽는 법:**
> - 상자 = 데이터의 25%~75% 구간 (IQR)
> - 상자 안의 선 = 중앙값 (50%)
> - 수염 = 상자에서 1.5 x IQR 범위
> - 수염 밖의 점 = 이상치(outlier)
> - 상자가 겹치지 않는 피처 → 분류에 유리!

데이터프레임에서 타겟(Target)은 머신러닝 모델이 예측하고자 하는 정답 열(Column)을 의미합니다. 
통계학에서는 종속변수, 수학에서는 Y라고도 부릅니다.


--느낀점 -- 

박스플롯 읽는것을 다시 좀 봐야겠다


파이썬 문법을 숙지할 필요성을 느낀다. 
개념으로 이해해도 실제로 코드를 따라치거나 파이썬문법을 응용해서 코드를 작성할줄을 모른다. 파이썬 문법을 제대로 모르기 때문이다.
이 상태에서는 머신러닝의 개념을 알아도 구동하지 못할거라는 생각이 든다.

주중에 학습할때는 힘들고 뭔말인지 모르는데 복습할때 조금 내것이 되는 것 같긴함
그래도 아직  정도만 이해가 갈 뿐이지 코드 순서라든지는 모르겠음.
TIL도 어떻게 작성하고 정리해야할지 모르겠음
 
--해야할 것 -- 
* 파이썬 코드작성을 먼저 언어로 해보자
그래프 그릴때 평균선 넣자


 

